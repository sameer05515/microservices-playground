# customer-service

Port: `8082`

Start from the service directory:

```bash
mvn spring-boot:run
```

Health:

```text
http://localhost:8082/actuator/health
```

See the root `README.md` for the complete V2 startup and testing sequence.
