/**
 * **Note:** These keys are just imported from chat-renderer project and not being used in this project.
 *
 * However we are retaining them for future reference.
 * They can guide use some possible key names required in local-storage.
 *
 */

export const APPLICATION_OBJECT_CHAT_RENDERER_KEY = "chatRendererConfig";
export const APPLICATION_OBJECT_THEME_KEY = "viewMode";

// Defined keys used in local storage
export const KEYS = {
  listVisible: "listVisible",
  selectedFileName: "selectedFileName",
  selectedConversationId: "selectedConversationId",
  viewMode: "viewMode",
  ConsolidatedReportV1_SidebarClosed: "ConsolidatedReportV1_SidebarClosed",
};
