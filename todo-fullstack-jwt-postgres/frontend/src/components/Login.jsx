import{useState}from'react';import{login,register}from'../api/authApi';export default function Login({onLogin}){const[u,setU]=useState('demo'),[p,setP]=useState('password'),[reg,setReg]=useState(false),[err,setErr]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setErr('');setBusy(true);try{const d=await(reg?register:login)({username:u,password:p});localStorage.setItem('accessToken',d.token);localStorage.setItem('username',d.username);onLogin(d.username)}catch(e){setErr(e.response?.data?.message||'Authentication failed')}finally{setBusy(false)}};return <div className="auth-card"><h1>{reg?'Create account':'Todo Login'}</h1><form onSubmit={submit}><input value={u} onChange={e=>setU(e.target.value)} placeholder="Username"/><input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="Password"/>{err&&<div className="error">{err}</div>}<button disabled={busy}>{busy?'Please wait...':reg?'Register':'Login'}</button></form><button className="link-button" onClick={()=>{setReg(!reg);setErr('')}}>{reg?'Already have an account? Login':'Create a new account'}</button><small>Demo: demo / password</small></div>}
