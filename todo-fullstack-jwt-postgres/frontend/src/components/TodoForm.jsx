import{useEffect,useState}from'react';export default function TodoForm({editingTodo,onSave,onCancel,loading}){const[t,setT]=useState(''),[d,setD]=useState('');useEffect(()=>{setT(editingTodo?.title??'');setD(editingTodo?.description??'')},[editingTodo]);const submit=e=>{e.preventDefault();if(!t.trim())return;onSave({...editingTodo?{id:editingTodo.id}:{},title:t.trim(),description:d.trim()});if(!editingTodo){setT('');setD('')}};return <form className="todo-form" onSubmit={submit}><input value={t} onChange={e=>setT(e.target.value)} placeholder="Todo title"/><textarea value={d} onChange={e=>setD(e.target.value)} placeholder="Description" rows="3"/><div className="form-actions"><button disabled={loading}>{loading?'Saving...':editingTodo?'Update':'Add Todo'}</button>{editingTodo&&<button type="button" className="secondary" onClick={onCancel}>Cancel</button>}</div></form>}
