import api from './api';export const getTodos=async({page,size,search,completed})=>(await api.get('/todos',{params:{page,size,...(search?{search}:{}),...(completed!=='all'?{completed}: {})}})).data;export const createTodo=async x=>(await api.post('/todos',x)).data;export const updateTodo=async({id,...x})=>(await api.put(`/todos/${id}`,x)).data;export const toggleTodo=async id=>(await api.patch(`/todos/${id}/complete`)).data;export const deleteTodo=async id=>api.delete(`/todos/${id}`)
